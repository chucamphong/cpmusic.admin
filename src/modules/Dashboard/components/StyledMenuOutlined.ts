import styled from "styled-components";
import { MenuOutlined } from "@ant-design/icons";

const StyledMenuOutlined = styled(MenuOutlined)`
    font-size: 18px;
    line-height: 64px;
    padding: 0 24px;
    cursor: pointer;
    transition: color 0.3s;
`;

export default StyledMenuOutlined;